<ResourceDictionary
    x:Class="Nagi.WinUI.Styles.InsightsTemplates"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:Nagi.WinUI.ViewModels"
    xmlns:controls="using:ImageEx">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="ms-appx:///Styles/Converters.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="SeeAllListItemStyle" TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
        <Setter Property="Padding" Value="0" />
        <Setter Property="MinHeight" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    </Style>

    <DataTemplate x:Key="TopSongItemTemplate" x:DataType="vm:TopSongItem">
        <Grid Padding="0,8" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Bind Rank.ToString()}" VerticalAlignment="Center"
                       HorizontalAlignment="Center" Foreground="{ThemeResource AppPrimaryColorBrush}"
                       FontWeight="Bold" FontSize="14" />
            <Grid Grid.Column="1" Width="40" Height="40" HorizontalAlignment="Center">
                <Border CornerRadius="4" Visibility="{x:Bind IsArtworkAvailable, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}">
                    <controls:ImageEx IsCacheEnabled="True"
                        Source="{x:Bind ArtworkUri, Mode=OneWay, Converter={StaticResource StringToImageSourceConverter}, FallbackValue=null}"
                        DecodePixelWidth="40" DecodePixelType="Logical" Stretch="UniformToFill" />
                </Border>
                <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4"
                      Visibility="{x:Bind IsArtworkAvailable, Converter={StaticResource InverseBooleanToVisibilityConverter}, Mode=OneWay}">
                    <FontIcon Glyph="&#xE8D6;" FontSize="16" Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                              HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
            </Grid>
            <StackPanel Grid.Column="2" VerticalAlignment="Center" Spacing="1">
                <TextBlock Text="{x:Bind Title}" TextTrimming="CharacterEllipsis" FontSize="13" FontWeight="SemiBold" />
                <TextBlock Text="{x:Bind Artist}" TextTrimming="CharacterEllipsis" FontSize="12"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <TextBlock Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Right"
                       Text="{x:Bind StatText}" FontSize="13" FontWeight="SemiBold"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="TopArtistItemTemplate" x:DataType="vm:TopArtistItem">
        <Grid Padding="0,8" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Bind Rank.ToString()}" VerticalAlignment="Center"
                       HorizontalAlignment="Center" Foreground="{ThemeResource AppPrimaryColorBrush}"
                       FontWeight="Bold" FontSize="14" />
            <Grid Grid.Column="1" Width="40" Height="40" HorizontalAlignment="Center">
                <Border CornerRadius="20" Visibility="{x:Bind IsArtworkAvailable, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}">
                    <controls:ImageEx IsCacheEnabled="True"
                        Source="{x:Bind ImageUri, Mode=OneWay, Converter={StaticResource StringToImageSourceConverter}, FallbackValue=null}"
                        DecodePixelWidth="40" DecodePixelType="Logical" Stretch="UniformToFill" />
                </Border>
                <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="20"
                      Visibility="{x:Bind IsArtworkAvailable, Converter={StaticResource InverseBooleanToVisibilityConverter}, Mode=OneWay}">
                    <FontIcon Glyph="&#xE77B;" FontSize="16" Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                              HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
            </Grid>
            <HyperlinkButton Grid.Column="2" Content="{x:Bind Name}"
                             Command="{x:Bind Command, Mode=OneWay}"
                             CommandParameter="{x:Bind}"
                             Style="{StaticResource InsightsHyperlinkButtonStyle}"
                             FontSize="13" FontWeight="SemiBold" />
            <TextBlock Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Right" FontSize="12"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="{x:Bind StatText}" />
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="TopAlbumItemTemplate" x:DataType="vm:TopAlbumItem">
        <Grid Padding="0,8" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Bind Rank.ToString()}" VerticalAlignment="Center"
                       HorizontalAlignment="Center" Foreground="{ThemeResource AppPrimaryColorBrush}"
                       FontWeight="Bold" FontSize="14" />
            <Grid Grid.Column="1" Width="40" Height="40" HorizontalAlignment="Center">
                <Border CornerRadius="4" Visibility="{x:Bind IsArtworkAvailable, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}">
                    <controls:ImageEx IsCacheEnabled="True"
                        Source="{x:Bind ArtworkUri, Mode=OneWay, Converter={StaticResource StringToImageSourceConverter}, FallbackValue=null}"
                        DecodePixelWidth="40" DecodePixelType="Logical" Stretch="UniformToFill" />
                </Border>
                <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4"
                      Visibility="{x:Bind IsArtworkAvailable, Converter={StaticResource InverseBooleanToVisibilityConverter}, Mode=OneWay}">
                    <FontIcon Glyph="&#xE93C;" FontSize="16" Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                              HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
            </Grid>
            <StackPanel Grid.Column="2" VerticalAlignment="Center" Spacing="1">
                <HyperlinkButton Content="{x:Bind Title}"
                                 Command="{x:Bind Command, Mode=OneWay}"
                                 CommandParameter="{x:Bind}"
                                 Style="{StaticResource InsightsHyperlinkButtonStyle}"
                                 FontSize="13" FontWeight="SemiBold" />
                <TextBlock Text="{x:Bind ArtistName}" TextTrimming="CharacterEllipsis" FontSize="12"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <TextBlock Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Right"
                       FontSize="13" FontWeight="SemiBold" Text="{x:Bind StatText}"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="TopGenreItemTemplate" x:DataType="vm:TopGenreItem">
        <Grid Padding="0,6" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Bind Rank.ToString()}" VerticalAlignment="Center"
                       HorizontalAlignment="Center" Foreground="{ThemeResource AppPrimaryColorBrush}"
                       FontWeight="Bold" FontSize="14" />
            <HyperlinkButton Grid.Column="1" Content="{x:Bind Name}"
                             Command="{x:Bind Command, Mode=OneWay}"
                             CommandParameter="{x:Bind}"
                             Style="{StaticResource InsightsHyperlinkButtonStyle}"
                             FontSize="13" FontWeight="SemiBold" />
            <TextBlock Grid.Column="2" VerticalAlignment="Center" FontSize="12"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="{x:Bind StatText}" />
        </Grid>
    </DataTemplate>

</ResourceDictionary>
